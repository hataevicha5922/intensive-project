// import { GenresType } from "../../api/api.interface";

export interface FilmCardProps {
  id: number;
  title: string | null;
  description: string;
  image: string;
  price: number;
  rating: number;
}
